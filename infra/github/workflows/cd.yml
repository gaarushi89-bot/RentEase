name: CD

on:
  push:
    branches: [ main ]

jobs:
  deploy-frontend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Deploy to Vercel
      run: echo "Deploying to Vercel... (Template)"
      # Add actual Vercel action here

  deploy-backend:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build and Push Docker Image
      run: echo "Building and Pushing Docker Image... (Template)"
      # docker build -t rentease-api ./apps/api
      # docker push ...
